[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [".venv", "samples"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]  # 行长限制由 formatter 处理

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
